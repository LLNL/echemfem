from setuptools import setup

setup(name="EchemFEM",
      version="0.0.1",
      description="FEM for electrochemical transport",
      packages=["echemfem"],
      install_requires=["pandas"],
      )
